#include <bits/stdc++.h>
using namespace std;
void countdown(int n) {
    if (n > 0) {
        cout << n << " ";
        countdown(n - 1);
    }
}
int main() {
    int n;
    cout << "Введите число: ";
    cin >> n;
    countdown(n);
    return 0;
}
